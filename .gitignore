node_modules/
docs/
req/
review assests/
dist/
build/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite/
.turbo/
.cache/
.idea/
.vscode/*
!.vscode/settings.json
*.tsbuildinfo
playwright-report/
test-results/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Repo cruft / local-only references
* copy.md
# claude/ holds tracked docs (specs, infra) AND local scratch. Track specs+infra,
# ignore the scratch subdirs.
claude/gold-os-complete/
claude/ui-screens/
claude/FIRST_PROMPT.md
.vercel
Thumbs.db

# tool logs
.client-dev.log
.npm-install.log
client/.vite-dev.log.err
client/.vite-dev.log.out

.vercel

# Claude Code local settings + plugin metadata (not for production)
.claude/
shared/*.js
client/vite.config.ts.timestamp-*
server/vitest.config.ts.timestamp-*

# Local DB backups (may contain real customer data — never commit)
backup-*.sql
backups/
*.dump

# Playwright MCP snapshots + root-level smoke-test screenshots
.playwright-mcp/
/transfers-completed-tab.png
/transfers-page-completed.png
# Detached dev-server log dir written by PowerShell Start-Process during local runs
.run/
